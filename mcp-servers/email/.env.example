# Gmail OAuth2 token file path (generated by python -m src.scripts.gmail_auth)
GMAIL_OAUTH_TOKEN_PATH=/path/to/token.json

# Set to "true" to enable dry-run mode (no emails are sent or drafted)
DRY_RUN=false
